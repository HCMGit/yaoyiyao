
<view class='index'>

<view wx:for="{{menbers}}" wx:key  style='transform:translateX({{-index*30}}rpx)'>

<image class='text' src='{{item}}' mode='aspectFill'></image>

</view>

  <text ></text>

</view>
 <view wx:if="{{hostid==userid}}" class="status text fr">
                <text class="delete" decode="true" bindtap='deleteComment' data-commentId="{{item._id}}">删除</text>
 </view>
<!-- images是图片地址 -->
